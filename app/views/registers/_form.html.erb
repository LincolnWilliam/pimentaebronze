<%= form_with(model: register, local: true) do |form| %>
  <% if register.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(register.errors.count, "error") %> prohibited this register from being saved:</h2>

      <ul>
        <% register.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="field form-group col-xs-10">
    <%= form.label :name %>
    <%= form.text_field :name , class: 'form-control' %> <!--#class: form-control pra aumentar o campo-->
    </div>
<div>

<div class="row">
  <div class="field form-group col-xs-2">
    <%= form.label :contact %>
    <%= form.text_field :contact, class: 'form-control' %>
  </div>
</div>

  <div class="field">
    <%= form.label :dateregister %>
    <%= form.date_select :dateregister %>
  </div>

  <div class="field">
    <%= form.label :birthday %>
    <%= form.date_select :birthday %>
  </div>

  <div class="row">
   <div class="actions col-xs-12 text-right">
     <%= form.submit 'Salvar registro', class: 'btn btn-primary'%>
   </div>
  </div>
<% end %>
